echo on

REM WARNING: assume boost, cmake, and python are installed (this is the case 
REM with appveyor)

REM TODO: OpenMP, pybind11

REM  Make sure Python works
python -c 'import sys; print(sys.platform, sys.version)'

python -m pip install --upgrade pip

REM NUMPY=$(python -c 'import sys; print("numpy" if sys.version >= "3.5" else "numpy<=1.16.4")')
REM python -m pip install -U ${NUMPY}
